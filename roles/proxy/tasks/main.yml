- name: Add new Nginx configuration
  template:
    src: templates/nginx.conf.j2
    dest: /etc/nginx/nginx.conf
    owner: root
    group: root

- name: Reload Nginx Configuration 
  service:
    name: nginx
    state: reloaded